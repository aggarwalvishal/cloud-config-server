global:
  message: Welcome to global Microservice environment.
logging:
  level:
    root: INFO
management:
  endpoints:
    web:
      exposure:
        inculde: health, info, refresh
  endpoint:
    health:
      show-details: always
